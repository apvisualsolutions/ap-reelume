# Tareas de ingeniería

Este es el registro canónico de **lo que la matriz de alcance no puede llevar**: herramientas,
puertas, deuda, defectos y preguntas sin medir. El alcance —lo que el programa hace para quien lo
usa— vive en [FEATURES.md](FEATURES.md) y **no se repite aquí**: cuando una tarea pertenece a una
fila de esa matriz, aquí sólo se la nombra.

Está en español y sin pareja inglesa a propósito: es el cuaderno de trabajo, no un documento que
alguien de fuera lea. La regla del bilingüismo es para lo que ve quien usa el programa.

## La regla del orden, y es la que gobierna este fichero

**La más vieja arriba. Lo nuevo se añade por abajo.** Se toma la primera de la lista que no esté
`PARADA`; si la de arriba lo está, se sigue leyendo hacia abajo.

**Por qué, dicho por el propietario el 2026-09-13**: quien lee esto —una persona o un agente— lee de
arriba hacia abajo y no llega al final. Con lo nuevo arriba, lo viejo no se hace nunca y la lista
crece hasta el fin de los días mientras entran tareas que tampoco se acaban.

**Los identificadores se reparten por fecha de nacimiento y no se reutilizan nunca**, así que los
huecos de la lista de abiertas son las que ya se cerraron y siguen abajo con su número. Una fila que
apareciera con un número menor que la de encima sería una tarea colada en la cabecera para que se
hiciera antes, y eso es lo que la guarda impide.

**La antigüedad es el valor por defecto, no una cárcel.** El propietario puede poner una tarea delante
de su turno, y entonces **la fila no se mueve de sitio** —la posición es la fecha de nacimiento, que es
lo que se puede comprobar— sino que cambia de estado a `ADELANTADA` y dice **desde cuándo y por qué**.
Decidido el 2026-09-13: sin eso, el orden le quitaría el volante a quien decide, y una prioridad que
vive fuera del registro es una que nadie cuenta — que es el agujero que este fichero vino a tapar. La
guarda exige el motivo, así que un adelanto silencioso no existe.

Lo hace cumplir `TareasRegisterTests`, y comprueba lo que una persona olvida: que las fechas y los
números van en orden ascendente hacia abajo, que cada fila lleva identificador, fecha y estado, que
ningún identificador se repite entre las dos listas, y que una fila `PARADA` nombra qué la para.

## Estados

| Estado | Significado |
| --- | --- |
| `ABIERTA` | Nadie la ha empezado. |
| `ADELANTADA` | El propietario la puso delante de su turno. **Va antes que cualquier `ABIERTA`**, y la fila dice desde cuándo y por qué. |
| `EN CURSO` | Alguien está en ella ahora. |
| `PARADA` | Hay un bloqueo nombrado. Se salta y se sigue leyendo. |
| `DEL PROPIETARIO` | No se resuelve programando: dinero, una credencial, un aparato o una decisión suya. |
| `POR COMPROBAR` | Puede que ya esté hecha; hay que medirlo antes de gastar en ella. |

## Abiertas

| Id | Nacida | Estado | Qué cambia | Detalle |
| --- | --- | --- | --- | --- |
| `ENG-002` | 2026-09-05 | `DEL PROPIETARIO` | Si un lector de pantalla lee peor los encabezados en mayúsculas, quien use el programa con uno lo nota. | Pregunta **sin medir**, y no la contesta nadie de memoria: se mide con un lector real antes de gastar en un mecanismo que una decisión anterior descartó por coste. Hasta entonces no es un defecto conocido, es una duda sin datos. |
| `ENG-005` | 2026-09-12 | `DEL PROPIETARIO` | Nada del árbol monta el anfitrión real de Windows, así que qué motor gráfico elige la aplicación que se distribuye no lo establece ninguna puerta. | El paseo «físico» también es headless. `PLY-016` lo necesitará el día que su cadena llegue a la tarjeta. Decidido que sea un ejecutable de diagnóstico pequeño; **abre una ventana**, así que se pide al propietario cuando no esté trabajando. |
| `ENG-020` | 2026-09-13 | `ABIERTA` | Cerrar la aplicación lanza una excepción y sale con código 82, así que quien la cierre puede ver un aviso de fallo de un programa que funcionaba bien. | Medido el 2026-09-13 abriendo la aplicación de verdad para juzgar la nitidez: reprodujo sin problemas y **falló al salir**. `WindowsTrayService.Dispose()` pone `_icon.IsVisible = false` desde el hilo que destruye el contenedor de servicios —lo llama `ApplicationHost.DisposeAsync()`—, y a esas alturas el bucle de la interfaz ya terminó: `The calling thread cannot access this object because a different thread owns it`. `Hide()` tiene el mismo cuerpo y el mismo riesgo. Ninguna prueba lo vio, y por qué no lo vio es la mitad interesante: `ADR-0005` dice que **nada del árbol monta el anfitrión real de Windows**, así que el ciclo de vida del proceso no lo mide ninguna puerta. Hay que arreglar el hilo **y** decidir qué puede afirmar una prueba sobre el cierre. |
| `ENG-022` | 2026-09-13 | `ABIERTA` | El reescalador propio ha llegado a su techo: más nitidez sólo se consigue con un escalón de tono al lado del canto, y eso pide otro algoritmo. | Medido el 2026-09-13 con tres diseños de shader distintos: **todo lo que no puede salirse del rango local se queda en un 13 %** más cerca de la verdad —lineal con cualquier fuerza 13,7 %, y un remapeo monótono 13,4 % con escalón exactamente 0—, mientras que el cúbico afilado llega al 35,7 % con un escalón de 16 niveles. Atarlo a una copia lineal con margen da 25,7 % con escalón 10, peor relación. **Toda la nitidez por encima del 13 % sale de los lóbulos negativos, y el escalón también.** Lo que consigue las dos cosas es interpolar **a lo largo** del canto en vez de a través: la familia dirigida por bordes (DCCI, NEDI; es lo que hace la primera pasada de FSR, que no se copia por la atribución). Son doce muestras o más y una decisión de coste que `UpscaleCostPolicy` existe para contestar. |
| `ENG-023` | 2026-09-13 | `ABIERTA` | El difuminado de la curva de tono tiene que ser lo último antes de la pantalla, y hoy no hay sitio donde ponerlo. | Construido y retirado el 2026-09-13 en veinte minutos. La curva de tono pierde niveles al escribirse en 8 bits, y el arreglo de manual es repartir la fracción entre píxeles vecinos. Hecho **en resolución de origen** el propietario lo vio al instante: «ahora aparecen un montón de cuadraditos en toda la imagen». Medido: la culpa **no** es la amplitud —el afilado no lo amplifica, el recorte del shader lo impide, sigue siendo un nivel— sino el tamaño: la pantalla es cuatro veces el fotograma, así que una celda de 8×8 sale como un bloque de 32×32 y un nivel repartido sobre esa área vuelve a leerse como banda. **Un difuminado va después del escalado.** Eso pide llevar la curva de tono al shader, que sólo corre cuando el reescalado está activo, así que hay que decidir qué pasa cuando no lo está. Lo que sí quedó puesto es el redondeo al nivel más cercano, que era un defecto aparte. |
| `ENG-024` | 2026-09-13 | `ABIERTA` | **Falta un reductor de ruido, y es lo que de verdad arregla lo que el propietario ve.** Sin él, subir la gamma saca a la luz los bloques que dejó la compresión. | Medido por el propietario el 2026-09-13 con el control definitivo: abrió el mismo fichero en VLC y «con la reducción de ruido se arregla casi todo, sólo faltaría la nitidez y el reescalado» — o sea, su imagen ideal es nuestro reescalado más un reductor de ruido. Los cuadros no los crea nuestro código: están en el fichero y la curva de tono los estira. **Y el complemento viaja en el paquete**: `libhqdn3d_plugin.dll` está en `videolan.libvlc.windows 3.0.23.1`, junto con `libgradfun` (quita bandas) y `libsharpen`. **La sonda que hay que hacer primero**: la evidencia `PLY16-low-res-spike.md` dice que los filtros de vídeo de VLC 3 nunca procesan un fotograma por la ruta de callbacks, pero eso se midió con un filtro que **cambia el formato** y falló en la compensación de formatos; `hqdn3d` no lo cambia, así que puede sobrevivir donde el otro no. **Esa vía se cerró el 2026-09-18 por licencia, no por técnica**: `hqdn3d.h` viene de MPlayer y es GPL (`ENG-027`), así que el complemento no puede viajar en un programa propietario. Si tampoco pasa, la salida es un filtro que preserve bordes dentro de nuestro propio shader, **antes** del afilado. Y lleva mando: por la regla 11 una opción que se juzga mirando el vídeo vive en el engranaje del reproductor. |
| `ENG-026` | 2026-09-14 | `ABIERTA` | Una prueba del paseo se pone roja sola, y eso enseña a relanzar CI sin mirar — que es como se cuela un rojo de verdad. | `AssembledPhysicalWalkTests.The_other_version_is_switched_to_with_the_mouse_and_its_question_answered` falló en CI el 2026-09-14 con `Expected 00:00:00, Actual 00:01:00`, en una tanda que **no tocó ni una línea** de `src/` ni de `tests/` — comprobado con `git diff --name-only 7e47a502 25e5ffec -- src tests`, vacío. Relanzada, pasó. **La hipótesis, que es de LECTURA y no está medida**: la prueba espera por una condición —que el cabezal baje de 10 s, `AssembledPhysicalWalkTests.cs:3656`— y luego afirma sobre **otra cosa**, la posición guardada en el almacén de vistos (`:3663`), que se escribe por su cuenta; entre las dos cabe el minuto anterior. Lo que hay que medir antes de tocar nada es si el almacén tiene forma de que alguien lo espere; si la tiene, la corrección es esperar por lo que se afirma, nunca una tolerancia. Tres evidencias ya nombran esta prueba, así que conviene leerlas primero: `audit-version-switch-reentry.md`, `audit-version-switch-question-guard.md` y `audit-walk-press-retry.md`. **Y no es la única**: el 2026-09-18 `The_courses_destination_marks_a_folder_and_carries_on_with_a_course` falló en la primera pasada de la puerta de accesibilidad (`Assert.False`, `AssembledPhysicalWalkTests.cs:6675`) y pasó en la segunda, en el run `35318637970` de un commit que sólo tocaba `eng/libvlc/verify-nogpl.ps1` y su flujo, que ese run no ejecuta. Dos pruebas del mismo paseo rojas por su cuenta en cuatro días apuntan a algo común del arnés antes que a cada prueba. |
| `ENG-029` | 2026-09-18 | `ABIERTA` | Los avisos legales no nombran todas las bibliotecas que van dentro del motor, y las que piden reproducir su copyright no lo tienen reproducido. | Los avisos dicen que el motor entero es LGPL-2.1, pero sus complementos enlazan bibliotecas de terceros con licencias propias —SRT es MPL-2.0, y hay BSD y MIT entre las ochenta de contrib—. Encontrado al leer `ENG-028`, que sólo cubrió las tres LGPL-3.0. Hay que sacar el inventario de lo que el paquete de fuentes del motor compila y enlaza, no de memoria, y con una puerta que falle ante una biblioteca sin aviso. |
| `ENG-030` | 2026-09-18 | `ABIERTA` | Con un vídeo HDR10, el reproductor dice «SDR» y no aplica la conversión de tono que `PLY-003` promete, aunque la matriz la da por verificada. | Construido y nunca conectado: `LibVlcVideoCapabilities.WithColourTransfer` reconoce la curva y **sólo la llaman las pruebas**; los cuatro sitios que construyen un `PlaybackRequest` en `src/` lo dejan con `SourceHdr` en `None`, así que `VideoOutputPolicy` decide siempre SDR. La prueba de HDR que `ENG-001` hizo correr en el servidor prueba la función, no el reproductor. Encontrado el 2026-09-18 al subir el suelo de ese archivo. Lo que hay que decidir es de dónde sale la curva al reproducir, porque LibVLC 3 no la expone en sus pistas y ffprobe no viaja; y hay que revisar la fila de `PLY-003` con lo que se mida. `Describe(VideoOutputDecision)`, en el mismo archivo, no lo llama nadie. |
| `ENG-031` | 2026-09-18 | `ABIERTA` | Una prueba de vídeo puede volver a saltarse en el servidor con el run en verde, que es como `ENG-001` pasó meses sin verse. | Lo encontró `gate-auditor` el 2026-09-18 al revisar el cierre de `ENG-001`. `CodecMatrixTests.RequireSampleAsync` omite una fila si no hay codificador o le falta uno, y nada limita cuántas omite `MediaTests` en CI; además el paso de `ci.yml` sale sin instalar nada si el runner ya trae un ffmpeg, así que el día que la imagen traiga otro build las omisiones vuelven en silencio. Lo que falta es un techo de omitidas por suite en CI, al estilo del contador de fases de `package-arm64.ps1`, con la lista de las que se aceptan y su motivo (hoy: el diagnóstico manual de `PictureAdjustmentOnRealFramesTests`). |
| `ENG-039` | 2026-09-19 | `ABIERTA` | Las dos baterías de los guiones del cierre no las corre nada por su cuenta, así que un cambio que rompa el acta o el filtro de privacidad puede entrar en verde. | `test-cierre-acta.ps1` y `test-cierre-privacidad.ps1` se corren a mano. La orden de baterías del manifiesto (`hooks.tests`) no la ejecuta ninguna herramienta del sistema común —buscado en sus guiones— y en CI no está la carpeta de herramientas compartidas, que la de privacidad necesita. La del acta no la necesita y podría ir en CI como prueba de `DocumentationTests`; la otra pide decidir con la sesión de IT dónde corre. Tocar el manifiesto cambia su huella y obliga a volver a adoptar. Encontrado al auditar las puertas de la adopción (`audit-adoption-gates.md`). |
| `ENG-040` | 2026-09-20 | `ABIERTA` | `UiTests` se pone roja sola con un fallo que no es del código, y cada aparición cuesta un run de cuarenta minutos y un relanzamiento a ciegas. | Tercera aparición medida de un `Test Case Cleanup Failure` —«the calling thread cannot access this object»— con la traza entera dentro de Avalonia (`HeadlessUnitTestSession.EnsureIsolatedApplication` → `Compositor..ctor` → `Dispatcher.VerifyAccess`) y **sin una sola línea de este árbol**. El 2026-09-19 tumbó el run `35476796671` de `8c74a380`, un commit que sólo toca los dos relevos: `git diff --name-only 1f05feb4 8c74a380 -- src tests` sale vacío, y el mismo código había pasado verde una hora antes. Nombró a `NextEpisodeOverlayTests.The_overlay_is_captured_in_both_languages`, que duró **1 ms y ni llegó a correr**: lo que falla es preparar la aplicación **para** ella, así que la prueba nombrada no es la culpable. Las dos anteriores —`TransportGlyphTests` el 2026-08-28 y `CandidateCardTests` en `audit-constructor-guard-sweep.md`— se midieron a una entre catorce pasadas y no se pudieron atribuir a nadie. **Cuarta aparición el 2026-09-20 en local**, con la traza idéntica y una cuarta prueba distinta, `HomeLayoutTests.A_recently_added_card_keeps_the_declared_cover_after_the_library_counts`, también de 1 ms: la segunda pasada del **mismo binario** dio 1.477 de 1.477. Cuatro pruebas distintas de cuatro áreas distintas con la misma traza es ya el dato que descarta perseguir a ninguna de ellas. **Quinta aparición el 2026-09-25 en local**, cerrando `ENG-018`: `HomeCardTests.Loading_the_same_thing_twice_announces_nothing_the_second_time`, 1 ms, misma traza; la repetición dio 1.497 de 1.497. Lo que falta no es culpar a una prueba: es saber **qué deja una ventana viva entre pruebas** en esa suite, porque las dos veces que se supo la causa era una ventana abierta a mano y no cerrada en un `finally`. Hermana de `ENG-026`, que es lo mismo en el paseo con otra firma. |
| `ENG-041` | 2026-09-20 | `ABIERTA` | Dos reglas escritas sobre cómo cuenta la puerta de cobertura se contradicen, y equivocarse de regla cuesta un run de cuarenta minutos. | El relevo del 2026-09-20 dice que la puerta **suma** las ramas de cada suite —«media rama aquí y media allá son dos de cuatro»—, y eso costó un rojo real. Pero el mismo día, midiendo `MetadataEditorViewModel.cs` con el JSON de coverlet de tres suites, la aritmética que **coincide con lo que CI dijo** (`100/95`) es la contraria: ramas distintas cubiertas en cualquier sitio da 46/48 = 95 %, mientras que sumar informe a informe da 79/96 = 82 %. Las dos afirmaciones no pueden ser ciertas tal como están escritas. Lo que falta por medir es en qué se diferencian los dos casos, y la vía está escrita en `CLAUDE.md`: `gh run download <id> -n test-results` trae los 20 informes, se fusionan con el mismo `reportgenerator` y se leen con la aritmética de `check-coverage.ps1` —así se reprodujeron los nueve suelos de `Domain` exactos el 2026-08-31—. Mientras no esté medido, la regla segura es la restrictiva: cubrir las dos mitades de una rama dentro de la misma suite. |
| `ENG-046` | 2026-09-20 | `ABIERTA` | El skill del cierre manda enlazar **toda** evidencia nueva desde la matriz de alcance, y para las evidencias de tareas de ingeniería eso es falso: manda a buscar un sitio donde no deben estar. | El paso 7 de `.claude/skills/cierre/SKILL.md` dice «Una evidencia nueva se enlaza **siempre** desde `docs/FEATURES.md`». Medido el 2026-09-20 al cerrar `ENG-015` y `ENG-016`: la puerta va en la **dirección contraria** —`EvidenceLinkTests` comprueba que exista lo que la matriz enlaza, no que toda evidencia esté enlazada— y el precedente lo confirma, porque la evidencia de `ENG-014` tampoco está en `FEATURES.md` y nada se queja. Las evidencias de tareas se enlazan desde su fila de este fichero; las de alcance, desde la matriz. La frase ya lleva un matiz parecido justo debajo para el manifiesto del MVP, corregido el 2026-09-13 por el mismo motivo, así que la corrección es la misma con otro sujeto. **Es documentación del cierre, no del producto**: vale medio folio y evita que el siguiente enlace una evidencia donde no toca o se quede pensando que se le ha olvidado algo. |
| `ENG-047` | 2026-09-20 | `ABIERTA` | La previsualización de suelos de cobertura puede volver a quedarse ciega **exactamente como antes de `ENG-016`**, y las pruebas que deberían impedirlo no llegan al sitio donde pasaría. | Lo encontró `gate-auditor` el 2026-09-20 sobre las cinco pruebas que `ENG-016` añadió, en copia aislada y con cada mutante comparado con el original antes de creerlo. Eran dos defectos de `eng/preview-coverage-floors.ps1`, **y el segundo se cerró el mismo día**: las tres consultas a git llevaban `2>$null` y nadie leía `$LASTEXITCODE`, así que un git que no podía contestar y un árbol limpio daban la misma salida y el guion salía 0 — medido sobre una copia sin `.git` y con un fuente nuevo delante, contestó `No floor moves and no new file falls short`, y no era hipotético, porque git rechazó operar dentro de la copia aislada por `dubious ownership` esa misma sesión. Ahora se niega a contestar, con su prueba y su mutante visto matarla. **Lo que queda abierto es el primero, y medirlo cambió lo que hay que hacer.** Las seis pruebas paran en `-ListNewFiles`, así que devolver la línea del informe al rango de commits —el defecto pre-`ENG-016` entero— las deja verdes. Parecía una sexta prueba y no lo es: el guion contesta `-ListNewFiles` **desde el directorio de trabajo** y luego hace `Push-Location` a este repositorio antes del camino del informe, de modo que **la costura y el camino preguntan a repositorios distintos** y ningún árbol de mentira alcanza al segundo — comprobado al intentarlo, con la guarda nueva sonando porque el SHA de la escena no existe aquí. Cerrarlo pide que los dos pregunten al mismo, y eso reescribe la escena sobre la que están montadas las seis. **Dos supervivientes menores, hacia el ruido y no hacia la ceguera**: quitar `--diff-filter=A` haría pasar por nuevo un fichero modificado, y quitar `--exclude-standard` metería en la lista lo que git ignora. |
| `ENG-048` | 2026-09-20 | `ABIERTA` | `ArchitectureTests` cuesta **8m37s** y la guía la anuncia como «~ 2 s», así que quien elija la suite afectada por su coste elige a ciegas — y son ocho minutos y medio en cada run de CI, con el margen hasta el corte contado. | Medido el 2026-09-20 en local, `--no-build`, 60 de 60 en 8m37s, frente a las «~ 2 s» de la tabla de `CLAUDE.md`. **No se pudo atribuir**: una pasada excluyendo `PreviewCoverageFloorsTests` seguía corriendo pasados once minutos cuando se cortó, así que **las cinco pruebas nuevas de `ENG-016` NO son el grueso** y acusarlas sería la hipótesis cómoda. Lo que falta es medir por prueba —`--logger "console;verbosity=detailed"` o el `.trx`— y luego decidir: puede ser una suite que creció y hay que contarlo, o una prueba que espera por algo. Y en cualquier caso la cifra de la tabla se corrige, porque un coste escrito y falso es peor que ninguno: el propio `CLAUDE.md` tiene la regla escrita para las duraciones de CI. |
| `ENG-049` | 2026-09-25 | `ABIERTA` | Con la película en la ventana, cada vez que los controles aparecen o se van el vídeo cambia de tamaño, y desde que se van solos eso pasa cada pocos segundos. | Encontrado al cerrar `ENG-018`: la barra de título, el carril y la columna **ocupan sitio** cuando se revelan (`PlayerPanelColumnTests` mide que el escenario gana 64 px de ancho al ocultarse), así que revelar y ocultar mueve la imagen. Antes pasaba una vez por pausa; con el reloj de `ADR-0014` pasa cada vez que alguien toca el ratón. Lo habitual en otros reproductores es que lo que aparece **flote** sobre la imagen, que es lo que `ADR-0010` ya dice de un suceso. **No se decide sin que el propietario lo vea**, porque cambia lo que se ve en cada película: se mide con él delante y después se elige. |

## Lo que vive en la matriz de alcance, y por qué no está aquí

Estas siguen abiertas, pero tienen su fila con criterio y evidencia. **Aquí sólo se las nombra**, o
volveríamos a tener la misma tarea en dos sitios:

- **`PRD-006`** (`IMPLEMENTED`) — la paridad con el prototipo: los 42 defectos en el orden del
  propietario, la rejilla fluida, el ritmo vertical de la tarjeta y lo demás que se midió contra el
  prototipo. Se bajó de `VERIFIED` a `IMPLEMENTED` y le faltan comprobaciones.
- **`PLY-016`** (`IN_PROGRESS`) — el indicador en pantalla, el interruptor por vídeo con memoria por
  serie o curso, la superresolución del fabricante y AMD.
- **`PLY-018`** (`IN_PROGRESS`) — la cifra de coste por fotograma que su criterio promete.
- **`CRS-006`** (`DESIGN_APPROVED`) — la banda de imagen de la tarjeta de curso, sacada de la primera
  lección y no de la que se esté viendo.
- **`PRD-002`**, **`PRD-003`**, **`REL-001`**, **`REL-004`** — las cuatro que no se resuelven
  programando: el certificado comercial de firma, la máquina ARM64, la ficha de la tienda y la
  comprobación de marca del nombre público.

## Hechas

Se quedan con su identificador y su fecha de nacimiento, en el mismo orden, para que se vea qué salió
de dónde y para que ningún número se reutilice. Lo que hicieron está contado en el changelog.

| Id | Nacida | Cerrada | Qué era |
| --- | --- | --- | --- |
| `ENG-001` | 2026-09-05 | 2026-09-18 | Las pruebas de vídeo que el servidor se saltaba. El paso a `ffmpeg-full` del 2026-09-05 destapó cuatro de cinco y la fila no se cerró; la quinta, reconocer un HDR10, se saltaba porque ffmpeg 9.0 no escribe la curva en el contenedor. La receta etiqueta los fotogramas con `setparams` y la prueba ya no se omite. En el servidor, run `35366789010` de `50ddf864`: `MediaTests` 200 de 201, con la de HDR pasada y la única omitida un diagnóstico manual. Destapó `ENG-030`. Evidencia `audit-ci-ffmpeg-build.md`. |
| `ENG-003` | 2026-09-05 | 2026-09-20 | **El orden de portadas que el `ADR-0009` decidió, construido entero en tres planes.** Nació sin fila en la matriz y la ganó el 2026-09-18 como `LIB-021`. El 1 separó la portada elegida de la del proveedor, con su columna, para que refrescar no la destruyera; el 2 sacó el fotograma del propio vídeo en segundo plano; el 3, hoy, hizo que el orden se pueda cambiar: sección propia en Ajustes con sus dos botones y su «Restaurar valores por defecto», y un desplegable en el editor de cada título que se salta el orden general guardando **la lista entera**, para que moverlo después no cambie lo que ese título tenía dicho. Migración 25, y en ella se midió que la versión del esquema tiene **cinco** afirmaciones y no tres. De paso, la excepción por título se construyó primero como un desplegable y **siete puertas distintas lo rechazaron o pidieron su decisión** —dos por un `TextBox` que la plantilla de `ComboBox` construye siempre, el paseo dos veces, la forma de las listas de opción, la guarda de constructores y la de comandos mudos—; hacerles caso dio el control correcto y **ninguna hubo que aflojarla**. Evidencia `LIB021-cover-order-setting.md`. |
| `ENG-004` | 2026-09-11 | 2026-09-13 | «La cifra de duración de un run», decisión del propietario que estuvo abierta. Zanjada: un dato que siempre va a estar desfasado no se guarda, se mide cuando alguien lo necesita. Vive en `eng/measure-ci-time.ps1`. |
| `ENG-006` | 2026-09-12 | 2026-09-12 | El primer eslabón de `PLY-016`: el intercambio de `UYVY` a `YUY2` al subir la textura. Evidencia `PLY16-uyvy-to-yuy2.md`. |
| `ENG-007` | 2026-09-12 | 2026-09-12 | El engranaje del reproductor con su maqueta aprobada, y el ajuste de imagen dentro. |
| `ENG-008` | 2026-09-12 | 2026-09-13 | El botón «Restaurar valores por defecto» en todos los grupos de opciones, por la misma clave de traducción. Salieron **doce** y no catorce, contados al escribir la lista. `UX-010`. |
| `ENG-009` | 2026-09-13 | 2026-09-20 | Correr `gate-auditor` en un worktree ponía roja `EvidenceLinkTests`. Su barrido lee `.claude/` **a propósito** —las skills y los agentes son documentos de este repositorio— y ahí es justo donde viven las copias de otras sesiones, así que leía la copia del **único** documento con permiso para citar la invención que la puerta persigue: la ruta de la copia no casa con la lista blanca y sus citas archivadas se leen como delitos nuevos. Reproducido antes de tocar nada fabricando `.claude/worktrees/probe-eng009/docs/evidence/stable/audit-prototype-fidelity-round-four.md` con una cita numerada: rojo con el mensaje exacto de la ficha. **Y era la tercera vez que este repositorio escribía la misma exclusión a mano**, con dos redacciones distintas —`QuotedFigureTests` por prefijo, `RunDurationFigureTests` por segmento—, así que la regla se mudó a `RepositoryLayout.IsInsideAnotherCheckout` con sus dos lecciones dentro: relativa a la raíz y nunca absoluta, y el prefijo entero en vez de un segmento suelto. La guarda nueva, `The_sweep_never_reads_another_sessions_checkout`, afirma contra una ruta fabricada **y** contra el barrido real, porque en CI no hay ninguna copia y una prueba que sólo mirase el barrido real pasaría allí sin medir nada; lleva además el suelo de 100 documentos, que es lo que caza una exclusión ensanchada de más. Mutante visto morir: sin la exclusión en `Documents()`, la guarda falla. Verde por los dos lados —118 de 118 con la copia presente y sin ella— y `ArchitectureTests` 55 de 55, que es quien más lee `RepositoryLayout`. El artefacto fabricado se retiró. |
| `ENG-010` | 2026-09-13 | 2026-09-20 | Los dos mandos del grupo de escaneo eran campos sin almacén y sin ningún lector en `src/`: quien los movía no cambiaba nada y nada se lo decía. Cerrada **con** `ENG-044`, que es lo que la fila pedía — cablearlos antes habría dado dos mandos que se guardan y siguen sin gobernar nada, porque la vigilancia que describen no se encendía. Ahora leen y escriben `IScanWatchSettings` sin campo propio, que es la mitad que importa: un campo que recuerda su propio valor es indistinguible de uno que la aplicación lee, y así fue como esto atravesó un rediseño, un paseo y una auditoría de `UX-010` sin que nadie lo notara. **Corrección de la propia fila**: decía que los dos mandos «se clasificaron fuera de la lista de grupos de opciones» y era falso desde antes de cerrarla — `OptionGroupTests` lleva el grupo «Scanning» dentro con su botón de restaurar, así que `UX-010` no había que tocarlo. |
| `ENG-011` | 2026-09-13 | 2026-09-20 | La velocidad de reproducción se olvidaba al cerrar. **Y medirla destapó un segundo defecto que esta fila no nombraba y que se nota más: mientras el programa siguiera abierto, la velocidad se arrastraba de una película a la siguiente** sin que nadie la pidiera, y la pantalla la mostraba como elegida. Los dos tenían la misma causa, y es el defecto de la casa otra vez —hermano de `ENG-044`, cerrado el mismo día—: la preferencia **ya existía de punta a punta** y nadie la usaba en ninguno de los dos sentidos. `PlaybackPreference.SpeedMultiplier` en `Domain`, resuelta por los tres ámbitos con `EngineDefaultSpeed` de reserva, escrita y leída por `PlaybackPreferenceRepository` en SQLite — y `grep` de `resolved.SpeedMultiplier` en `src/` daba **cero**, con `resolved.Picture` de control positivo dando uno. **Lo que lo explicaba estaba escrito al lado desde hacía semanas**: el comentario de `ApplyPlaybackPreferences:70` dice que la imagen se aplica siempre «porque el motor sobrevive al fichero — es un singleton —, así que un ajuste del último film se llevaría a un film que nadie ajustó». `ControlPlayback` también es singleton y nadie unió las dos ideas. **Construido**: `ApplyAsync` aplica ahora la velocidad resuelta **siempre, el silencio incluido** —silencio resuelve a la velocidad normal del motor, nunca a «lo que siga corriendo»—; `ControlPlayback` gana un gancho de persistencia con el valor **recortado**, como ya hacía el de la posición, y un `AdoptSpeedAsync` para que su copia coincida con lo que el motor está haciendo, porque si no la pantalla miente en los dos sentidos. **Ámbito global, y es decisión y no el camino corto**: la velocidad es de cómo ve la persona, mientras que el idioma de una serie es de la obra — por eso las pistas llevan tres ámbitos y esto no; el mecanismo sigue ahí si algún día hace falta afinarlo. **Mutante visto morir**: aplicar la velocidad sólo cuando algún ámbito contestó deja pasar el arrastre, y la prueba que lo cubre cae. `AdoptSpeedAsync` llega con su consumidor en el mismo cambio, que es la mitad que este repositorio olvida. |
| `ENG-012` | 2026-09-13 | 2026-09-13 | El escalador propio de `PLY-016`, que ya dibuja: la rampa de un borde ampliado baja de 4 píxeles a 2. Evidencia `PLY16-portable-upscaler.md`. |
| `ENG-013` | 2026-09-13 | 2026-09-18 | **Lo que bloqueaba publicar: el motor de vídeo ya no lleva GPL.** La aplicación y los dos paquetes dejan el NuGet `VideoLAN.LibVLC.Windows`, que llevaba catorce complementos GPL, y llevan LibVLC compilado desde VLC 3.0.23 como lo compila VideoLAN —su `build.sh` dentro de sus imágenes de CI, en `.github/workflows/libvlc-nogpl.yml`—, con la GPL apagada en las bibliotecas de terceros, FreeType bajo la FTL (`ENG-025`), los módulos con fuente GPL retirados por `verify-nogpl.ps1` leyendo sus fuentes (`ENG-027`), y dos parches con el aviso con fecha que pide la LGPL-2.1 §2(b). Publicado por `libvlc-publish.yml` como prerelease `libvlc-3.0.23-nogpl.1` de este repositorio —el sitio lo decidió el propietario; `releases/latest`, que es lo que pregunta el actualizador, sigue en 404—, fijado por SHA-512 en `eng/libvlc/libvlc.lock.json` y descargado por `eng/libvlc/LibVlc.targets`, que rechaza cualquier otro byte. Las diez suites pasan contra él, el MSIX lleva exactamente el árbol verificado (`LibVlcPayloadTests`), y su código fuente —parches, guiones y los tarballs de terceros— viaja con cada versión. CI verde en `cd813b3c` (run `35344667881`). Evidencias `ENG013-nogpl-build-spike.md`, `ENG013-reproducible-build.md` y `ENG013-app-on-own-engine.md`. Queda `ENG-028`. |
| `ENG-014` | 2026-09-13 | 2026-09-14 | Tres afirmaciones falsas o incompletas sobre qué plugins de LibVLC son GPL. Cerrada con el barrido completo de los ~300 plugins (no una muestra): `libavcodec_plugin.dll` y `libswscale_plugin.dll` llevan `--enable-gpl` por compartir el mismo build de FFmpeg, `libx26410b_plugin.dll` es GPL por licencia propia de x264, ningún otro. Corregidos `LEGAL.{es,en}.md`, `THIRD-PARTY-NOTICES.{es,en}.md` y `NOTICE-VideoLAN.txt`. Evidencia `audit-eng014-plugin-gpl-scan.md`. |
| `ENG-015` | 2026-09-13 | 2026-09-20 | La cabecera del trinquete del paseo se contradecía con el trinquete, y **el propio guardián era lo que lo sostenía**: el hook denegaba toda escritura sobre `eng/walk-pending.txt` sin distinguir entre añadir una fila —que debe impedir— y corregir un comentario caducado. Cerrada por el lado del guardián, que es lo que la fila pedía: el bloque en línea de `.claude/settings.json` pasó a `.claude/hooks/pre-write-ratchets.sh`, que deniega si el cambio toca una FILA y deja pasar si sólo toca comentarios `##`. Cubre los cortes a media fila, y `eng/coverage-debt.txt` conserva su denegación total porque ahí no hay nada que distinguir. **Medido por tubería con doce casos —siete que suenan y cinco que callan— y dos mutantes**: uno que nunca deniega rompe los siete, otro que cuenta los comentarios como filas rompe dos de los cinco. Y medido después **en la aplicación**, con el par de controles: la corrección del comentario pasó y la fila nueva quedó denegada. **La secuencia real del trinquete se reconstruyó con `git log -G`** —`-S` no la ve, porque la cadena no cambia de número de apariciones cuando cambia sólo la cifra—: 0 el 2026-08-18, **20** el 2026-08-25, 22 y después 23 el 2026-09-02. Con eso, los textos desfasados eran **dos y no tres**: las líneas 5 y 9 del fichero; la 53 estaba completa porque la 65 cuenta la segunda subida del mismo día. La cuarta corrección estaba en `CLAUDE.md`, y ésa era **falsa y no vieja**: decía que subió a 23 el 2026-08-25 cuando subió a 20, con el `<!--medido:paseo-pendiente-->` intacto sobre el 23 de hoy. La batería vive versionada en `.claude/hooks/pre-write-ratchets.test.sh`, no en un temporal, porque un hook que calla no deja rastro en el registro. Evidencia `ENG015-walk-ratchet-guard.md`. |
| `ENG-016` | 2026-09-13 | 2026-09-20 | La previsualización de suelos de cobertura callaba sobre los archivos nuevos sin commitear, que es justo cuando alguien la usa: buscaba con un rango de commits, que sólo nombra lo que **ya está en un commit**. Ensanchada con lo preparado (`--cached`) y lo que git no sigue todavía (`ls-files --others`); la puerta **no se toca**, porque en CI el árbol está commiteado por definición y allí el rango es correcto. La detección se extrajo a `Get-NewSourceFile` con un modo `-ListNewFiles`, **y eso es la mitad del trabajo**: una aserción que leyera el guion buscando «ls-files» seguiría verde mientras la consulta que nombra no corriera en ningún sitio, que es la puerta ciega que este repositorio no para de encontrar. `PreviewCoverageFloorsTests` levanta un repositorio de mentira y mide por efecto, con **cinco casos y su contrario al lado**: sin seguir, preparado, ya commiteado contra la base —la fuente original, que nadie más notaría si se perdiera—, un árbol limpio que no debe listar nada, y un `.md` nuevo que tampoco. **Vista fallar con el guion mutado a su versión vieja**: rompe exactamente las dos de las fuentes nuevas y deja verdes las otras tres. Evidencia `ENG016-preview-new-files.md`. |
| `ENG-017` | 2026-09-13 | 2026-09-20 | Nada comprobaba que el escalador compilara su shader una sola vez por película, y no por descuido: `TryCompile` es estática, el centinela es privado, el control nace desde XAML sin parámetros y el árbol no tiene `InternalsVisibleTo`, así que **nada de fuera de la clase podía contar nada**. Cerrada con la costura que la fila pedía: `VideoFrameView.ShaderCompiler`, una propiedad con delegado cuyo valor por defecto es el compilador de la aplicación — la forma que esta misma carpeta ya usa en `AudioOutputViewModel.SelectionHandler` y `PlayerViewModel.GestureHandler`. **Y no es un adorno registrado-y-nunca-alimentado**, que es la objeción que se llevó por delante a `WithoutWhatItCannotAfford`: abre además la rama en que el shader **no** compila —un driver que rechaza el SkSL—, inalcanzable desde este control en una máquina donde sí compila. Es el mismo argumento, un escalón más arriba, que el parámetro opcional de `TryCompile` lleva escrito dentro. **Tres pruebas y tres controles vistos sonar**: el centinela invertido da 3 compilaciones en vez de 1; el respaldo devolviendo nulo rompe la que afirma que un control sin compilador dibuja **los mismos píxeles** que uno al que se le entrega el de la aplicación —con un tercero que lo rechaza como control negativo, sin el cual serían dos mitades del mismo error coincidiendo—; y el suelo antiblindaje suena con tres fotogramas iguales, que es como esta prueba se volvería ciega: una escena dibujada una sola vez también contaría uno. `UiTests` 1.477 de 1.477 y `ArchitectureTests` 60 de 60. |
| `ENG-018` | 2026-09-13 | 2026-09-25 | Los mandos del reproductor no se comportaban como los de cualquier reproductor: un movimiento del ratón los dejaba fijos hasta la siguiente pausa, el puntero no se ocultaba, el clic en la imagen y la rueda no hacían nada y Esc sólo salía de pantalla completa. **La decisión que lo sostenía se reabrió con su coste pagado, no esquivado** (`ADR-0014`): el reloj es el `IClock` de la aplicación y las pruebas le pasan uno que avanzan a mano, y el paseo lo pone en infinito salvo en una escena que lo baja a 200 ms y mide con el motor real que los controles se van solos. Ahora se van a los 3 s salvo con panel o engranaje abiertos, con el puntero sobre un control o en pausa; el puntero se va con ellos; clic en la imagen pausa y reanuda; la rueda mueve el volumen; Esc retrocede una capa como en el prototipo. **Doce mutantes muertos**, y el paseo destapó un defecto real: con el Esc nuevo, cerrar un desplegable del engranaje cerraba el engranaje entero. Dejó `ENG-049`. [Evidencia](evidence/stable/audit-eng018-player-chrome.md). |
| `ENG-019` | 2026-09-13 | 2026-09-13 | El instrumento que faltaba para juzgar la nitidez: la distancia a una verdad dibujada a propósito, que castiga a la vez el borrón, el dentado y el halo. Con él, la imagen pasó de un 30,4 % a un 35,7 % más cerca de la verdad. Evidencia `PLY16-upscale-fidelity.md`. |
| `ENG-021` | 2026-09-13 | 2026-09-13 | Los cuadros al subir la gamma: **cerrada como diagnóstico, no como arreglo**. El control lo puso el propietario en VLC y son ruido de compresión del fichero, no del programa; lo que arregla eso es un reductor de ruido (`ENG-024`). De paso se corrigió un defecto real que estaba al lado: la curva truncaba en vez de redondear, y con brillo 0,1 dejaba la imagen entera medio nivel oscura. El difuminado que se construyó salió peor y está retirado (`ENG-023`). Evidencia `PLY18-tone-banding.md`. |
| `ENG-025` | 2026-09-14 | 2026-09-14 | La cláusula de publicidad de FreeType, **aceptada por el propietario el 2026-09-14**: se elige la FTL y no la GPL, así que los subtítulos se podrán dibujar cuando el motor se compile sin GPL. El reconocimiento que la cláusula exige tiene sección propia en `THIRD-PARTY-NOTICES.{es,en}.md`, y `ADR-0013` lleva la enmienda. **De paso cerró un incumplimiento que ya estaba abierto**: FreeType viaja hoy dentro de Skia y no se le reconocía. |
| `ENG-027` | 2026-09-18 | 2026-09-18 | Los documentos legales decían que el paquete lleva tres complementos GPL y **son catorce en x64 y once en ARM64**, por tres causas: FFmpeg compilado como GPL, módulos del propio VLC con fuentes GPL, y `libts` con la biblioteca aribb24 enlazada. Las cifras anteriores salían de buscar `--enable-gpl`, que sólo escribe FFmpeg. Leído en las fuentes de VLC 3.0.23 con el guion nuevo `eng/libvlc/scan-plugin-licenses.ps1`, que sigue los `#include` y falla antes que dar una lista corta. No se ha distribuido nada: cero releases. Corregidos `LEGAL`, `THIRD-PARTY-NOTICES` y `NOTICE-VideoLAN`. Cambia lo que tiene que hacer `ENG-013` y cierra la vía `hqdn3d` de `ENG-024`. Evidencia `audit-eng027-plugin-gpl-sources.md`. |
| `ENG-028` | 2026-09-18 | 2026-09-18 | **La LGPL-3.0 de gmp, nettle y live555, leída, y la licencia del programa corregida.** Van enlazadas dentro de cinco complementos del motor, medido en los binarios de las dos arquitecturas, y el §4 se cumple con el fuente que ya viaja. Faltaban en el paquete los textos de la LGPL-3.0 y la GPL-3.0 (van desde `50ddf864`, con prueba). Y salió lo que valía más: la LGPL-2.1 §6 —que ya aplicaba a LibVLC— y la LGPL-3.0 §4 exigen que el programa se pueda modificar para uso propio y depurar con ingeniería inversa, y `LICENSE` lo prohibía; el propietario aprobó la excepción, que va en `bf4fa9c9` con una prueba vista fallar sin ella. CI verde en `bf4fa9c9` (run `35377581606`). Conclusión en `LEGAL`; destapó `ENG-029`. |
| `ENG-032` | 2026-09-19 | 2026-09-19 | El repositorio público nombraba servicios internos de la casa: `.claude/settings.json` llevaba desde el 2026-09-05 el servidor del cajón común de la memoria compartida siete veces —una habilitación y seis denegaciones de escritura— y el skill de cierre, el del cajón propio. **La habilitación nunca hizo nada**: sólo actúa sobre servidores que declara `.mcp.json`, y ése no lo declara, así que el cajón común **no ha estado conectado aquí nunca**. Las denegaciones pasan a la configuración de usuario, donde una denegación gana desde cualquier ámbito, y el skill deja de escribir el nombre; una ruta de ejemplo de la batería del acta cambia de unidad y la batería da la misma salida, byte a byte. Medido por efecto en una sesión nueva: el servidor del proyecto sigue conectado y contesta; una denegación de prueba sobre una herramienta suya la retira de la sesión, y sin ella responde. El historial publicado no se reescribe, por decisión del propietario. |
| `ENG-033` | 2026-09-19 | 2026-09-19 | La marca del cierre vivía en el directorio de git de la copia de trabajo, y desde una copia paralela no se veía. Con la adopción del sistema común la pone `closing-marker.ps1` en el directorio **común** de git, con sesión, proceso y caducidad de cuatro horas, y la miran las dos puertas del plugin. El guardián de push propio se retiró en el mismo commit, después de ver pasar su batería de diez casos y morir su mutante. Evidencia `audit-adopt-common-system.md`. |
| `ENG-034` | 2026-09-19 | 2026-09-19 | El ajuste local de Claude Code sólo lo ignoraba la configuración global de git de esta máquina, y en un clon nuevo se habría podido publicar. `.gitignore` lo lleva desde la adopción del sistema común. Medido con `git -c core.excludesFile=/dev/null check-ignore -v .claude/settings.local.json`: antes salía 1 (nadie lo ignora) y ahora responde `.gitignore:55`; el control negativo, `.claude/settings.json`, sigue sin ignorarse. |
| `ENG-035` | 2026-09-19 | 2026-09-19 | El relevo de 9.134 líneas. Se congeló entero en `docs/NEXT-SESSION-HISTORY.{es,en}.md` —en `docs/` y no en una subcarpeta, porque lleva unos trescientos enlaces relativos que una subcarpeta rompería, y reescribirlos a máquina es retocar la historia— y el relevo vivo se sobrescribe en cada cierre. Tope y paridad en `eng/check-handoff.ps1`: 80 líneas y 6.144 bytes por idioma, fecha en la primera línea y la misma estructura de encabezados. Lo prueba `HandoffLimitsTests`, visto sonar contra el relevo viejo con siete hallazgos. Los enlaces que lo trataban como historia —la matriz y una auditoría— apuntan al congelado. |
| `ENG-036` | 2026-09-19 | 2026-09-19 | La variable `AP_SHARED_TOOLS` no existía y las comprobaciones de IT del cierre salían 2. Con el sí del propietario vive en el bloque `env` del ajuste local de Claude Code, que no se versiona, y apunta a la carpeta de herramientas del espejo del catálogo común. Medido por efecto en una sesión nueva: `Test-Path` del primer guion da `True`, y el plugin responde `pong — ap-smart-tech 0.7.4`. |
| `ENG-037` | 2026-09-19 | 2026-09-19 | El acta daba por ejecutadas las tres comprobaciones de IT por el texto del comando, y en aquel cierre iban dentro de una rama no tomada. Ahora las corre `cierre-compartidas.ps1`, que deja en el directorio común de git la salida con `EXIT=<código>` y la hora, y el acta lee ese fichero: tiene que existir, ser posterior a la marca y no haber salido 2. De paso lleva la regla del paso 0 que hacía el guardián retirado, contando también lo no añadido, y escribe el recibo que leen las puertas del plugin. Batería de 25 casos, entre ellos el de la rama no tomada, y tres mutantes muertos. Evidencia `audit-adopt-common-system.md`. |
| `ENG-038` | 2026-09-19 | 2026-09-19 | El recibo del acta escribía `"escapes": null` sin saltos, y con uno solo lo guardaba suelto en vez de en una lista: la causa era un `if` usado como valor, que en PowerShell desenrolla su salida. Ahora siempre es una lista y limpia los nulos de recibos viejos. Con el auditor de puertas se cerraron además diez huecos de la batería del acta, dos de `HandoffLimitsTests` y la sonda de privacidad, que no ejecutaba el guion del cierre; cada uno con su mutante visto sobrevivir antes y morir después. Evidencia `audit-adoption-gates.md`. |
| `ENG-042` | 2026-09-20 | 2026-09-20 | La documentación llamaba «complemento» a lo que el fabricante llama `plugin`. **El propietario devolvió la decisión —«no sé qué es correcto»— y con razón: no es suya**, porque las preguntas de licencia de este proyecto se cierran por ingeniería y ésta era de terminología. **Gana `plugin`, y el motivo no es la traducción sino la trazabilidad**: `LEGAL.es.md` argumenta sobre el §4 de la LGPL que «la obra combinada es cada complemento», y quien quiera comprobar esa afirmación tiene que poder cruzar la palabra con lo que hay delante, que se llama `plugin` en todos los sitios — la carpeta `plugins\` que `LibVlc.targets` empaqueta, los ficheros `*_plugin.dll`, el guion `scan-plugin-licenses.ps1` que produce la evidencia, y el texto **inglés del mismo documento**. `THIRD-PARTY-NOTICES.es.md` llegó a escribir «los complementos de `plugins/`» en una sola frase. Un documento legal cuyo término no casa con el artefacto que describe es más débil, no más claro. **Eran 32 y no 31**: contadas por forma y **revisadas una a una** antes de tocar nada, porque un barrido por patrón se lleva por delante cualquier uso con otro sentido; no había ninguno. Sólo los tres documentos que viajan con el paquete; el changelog publicado, el historial de relevos y las evidencias archivadas **no se tocan**, que son registro de lo que se dijo. Las tres parejas inglesas ya decían `plugin`, así que esto las alinea. **El límite, declarado**: no se pudo consultar a VideoLAN —`videolan.org` rechaza la conexión desde esta máquina, con `example.com` de control positivo respondiendo—, así que **no está comprobado qué término usa el fabricante en español**; el criterio de trazabilidad no depende de eso, pero si alguien llega a la traducción oficial y dice «complemento», merece releerse. |
| `ENG-043` | 2026-09-20 | 2026-09-20 | Los nombres de dos herramientas internas del sistema común publicados en este repositorio público por el relevo del 2026-09-20 (`docs/NEXT-SESSION.es.md:42` en `8c74a380`). **Decisión del propietario el 2026-09-20: aceptarlo, sin renombrar y sin tocar el historial.** El porqué, para que nadie la reabra por costumbre: un nombre de comando no es una credencial ni una dirección, así que no abre nada; medido con `gh repo view --json forkCount,stargazerCount`, el repositorio tiene **0 copias y 0 seguidores**, de modo que la difusión real es la que GitHub conserva y no una que se pueda recuperar; renombrarlas rompería lo instalado en todos los proyectos y obligaría a volver a adoptar, dejando el nombre viejo publicado igual; y reescribir el historial contradice `ENG-032`. **Lo que sí queda en pie es la guarda**: ningún documento de este árbol nombra una herramienta del sistema común —se describen—, y los nombres viven en el ajuste local, que no se versiona. |
| `ENG-044` | 2026-09-20 | 2026-09-20 | **La vigilancia de carpetas estaba construida entera y no se encendía nunca**, así que un vídeo copiado con la aplicación abierta no aparecía hasta reiniciarla. Lo único que la activaba era `ScanPolicy.Continuous`, y **nada de `src/` lo asignaba**; ninguna pantalla lo ofrecía. Cierra también `ENG-010` —los dos mandos de Ajustes eran su síntoma— y devuelve `LIB-003` a `VERIFIED`. **El segundo defecto, que no estaba en la fila y se encontró al terminarla**: el barrido de respaldo tampoco corría para nadie, por la misma bandera, así que no había recuperación para USB ni NAS —la otra mitad de lo que `LIB-003` promete— ni reintento de un vigilante caído, porque el coordinador cierra su canal de reintento en cuanto el horario se seca; el comentario de `RootWatchBackground` que afirmaba que «nunca se seca» era falso. **Lo construido**: `ScanWatchPolicy` decide las dos cosas en `Domain` —`ShouldWatchLive` y `ShouldSweepPeriodically`, con su asimetría escrita: el ajuste dice «raíces locales» y por eso no puede apagar el barrido de un disco extraíble, donde es la única red—; `IScanWatchSettings` guarda las dos respuestas y avisa al cambiar; el interruptor **surte efecto al momento**, porque `RootWatchBackground` rehace la vigilancia por generaciones y toda raíz vuelve a pasar por la misma decisión, de modo que la promesa escrita de `DeclareCourseFolder` se cumple también en ese camino; y el intervalo pasa a vivir en un solo sitio con **15** minutos, que es lo que el código ejercía y la evidencia de WP-2 respalda, frente a los **30** que la pantalla enseñaba sin gobernar nada. **La guarda contra la recaída, que es lo que faltaba**: la escena del paseo físico siembra ahora su raíz con `Startup \| Manual` —la política que las vías reales producen—, y la del alta con el ratón comprueba que la carpeta añadida es una que el ajuste resuelto del contenedor vigila; las dos vistas caer contra el código anterior. Se retiró además una aserción textual de `RootWatchWiringTests` que buscaba el nombre de una constante en el fuente de la composición y **estuvo verde durante todo el tiempo que el barrido no corrió para nadie**; la sustituye una que resuelve el planificador del contenedor real y lo hace emitir. **Dos guardas ciegas encontradas de paso y arregladas**: una prueba del `Dispose` que seguía verde sin la desuscripción, porque el apagado frenaba el reinicio por otra vía —ahora afirma la suscripción y no los vigilantes—, y una guarda de «¿ya hemos salido?» que ninguna prueba podía provocar, retirada en vez de dejarla sin medir. Y el doble del ajuste, que eran cinco copias de las mismas seis líneas en cinco clases, vive ahora en `tests/Shared`. Evidencia releída en `audit-wp2-assembly.md`. |
| `ENG-045` | 2026-09-20 | 2026-09-20 | Contar CRLF con `grep` siempre dice cero, y se había leído mal **dos veces** en este árbol: `grep` trata el CR como **fin de línea**, así que `grep -c $'\r'` devuelve 0 sobre cualquier fichero tenga los que tenga, y `od -c` canalizado a `grep` cuenta líneas de la salida de `od` y no bytes. Se cuenta con `tr -cd '\r' \| wc -c`. La primera vez fue el 2026-08-29 y quedó escrita en la memoria del proyecto; la segunda el 2026-09-20, comparando el artefacto `coverage-debt` con el árbol, donde las dos lecturas dieron 0 sobre un fichero con 402 — lo cazó un `diff` que dijo `1,402c1,402` y unos tamaños separados por exactamente 402 bytes, no el acordarse. **Nace y se cierra el mismo día porque NO vive aquí**: una regla olvidada dos veces es un hook que falta, y la trampa no es de este proyecto, así que se propuso a IT y la aceptaron como **regla R12** de su guardián, con el recambio en el mensaje, la batería de tres que suenan y cuatro que callan, el mutante que debe romper los tres y la vía de escape `#guard-ok`. Queda registrada aquí para que nadie la vuelva a abrir sin saber dónde está. |
