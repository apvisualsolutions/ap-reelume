## The command controls the autonomous walk has not pressed yet.
##
## One identity per line — the view that declares the control, then the anchor the walk aims at —
## followed by the reason it is still here. The list may only shrink: eng/check-walk-coverage.ps1
## refuses a line the walk now presses, refuses a control missing from both halves, and refuses a
## list longer than its ratchet. The batches are the ones decided in docs/NEXT-SESSION.es.md.
##
## A control leaves this file when the walk presses it with the mouse, asserts on the effect, and
## clicks beside it to no effect — all three, which is what PressAsync does and records.

EpisodeRowView#EpisodePlayAction                           # batch 1: the series card, which needs the show, season and episode rows seeded
LooseFileBanner#LooseFileAddFolderAction                   # batch 2
LooseFileBanner#LooseFileCancelAction                      # batch 2
LooseFileBanner#LooseFileConfirmAction                     # batch 2
MovieDetailsView#MovieResumeAction                         # batch 1: shown only with stored progress worth returning to
MovieDetailsView#MovieTrailerAction                        # batch 1: shown only when a trailer file sits beside the film, which needs a version group the walk does not seed yet
PlayerView#PlayerRecoveryOpenExternally                    # batch 2
PlayerView#PlayerRecoveryRetry                             # batch 2
VersionSwitchDialog#VersionSwitchCancel                   # batch 2d: needs the question raised a second time; measured a nameless disabled Button in the way
VersionSwitchDialog#VersionSwitchRestart                  # batch 2d: needs the question raised a second time; measured a nameless disabled Button in the way
